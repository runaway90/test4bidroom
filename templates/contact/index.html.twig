{% extends 'base.html.twig' %}


{% block body %}
    {#<ul>#}
        {#{% for error in errors %}#}
            {#<li>{{ error.message }}</li>#}
        {#{% endfor %}#}
    {#</ul>#}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <h1>Test task for Bidroom.</h1>
    <h2>Simple form to write in DB</h2>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

    <h2>Form to send email</h2>

        {{ form_start(form_send, {'action': path('send_first_simple_form_variant'), 'method': 'POST'}) }}
        {{ form_widget(form_send) }}
        {{ form_end(form_send) }}
</div>
{% endblock %}
